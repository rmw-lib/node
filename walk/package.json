{
  "name": "@rmw/walk",
  "version": "0.0.6",
  "repository": "https://github.com/rmw-lib/node/tree/master/walk",
  "author": "i@rmw.link",
  "license": "AGPL",
  "exports": {
    ".": "./lib/index.js",
    "./*": "./lib/*"
  },
  "files": [
    "lib/*"
  ],
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "prepare": "rm -rf lib && rsync -av --include='*/' --include='*.js' --include='*.mjs' --exclude=* src/ lib/ && npx coffee --compile --output lib src/ && npx md-include .markdown.json"
  },
  "type": "module"
}
